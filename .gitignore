# Session context (contains private brand info)
context/session-context.md
context/session-context-log.md
context/session-context-archive.md
context/kol-exclusion-list.md
context/brand-context.md
context/brand-kol-strategy.md
context/confirmed-markets.md
context/pipeline-state.md
context/presentation-design-system.md
context/design-context.md
context/ops-context.md

# Proof files (can be large)
proofs/

# Output drafts
output/

# Jarvis's Session Context
claude-session-context.md

# macOS
.DS_Store
**/.DS_Store

# Environment — secrets only; template is committed
.env
.env.*
!.env.example

# Secrets — never commit
.claude/settings.local.json

# Local tool config
.playwright-mcp-config.json

# Archived — old versions, stale files
.claude/agents/_archived/

# Playwright MCP logs and traces
.playwright-mcp/

# Research snapshots (intermediary scratchpad files from agent runs)
research-snapshots/

# Brand fonts — each brand owner brings their own
fonts/

# Shopify codebase — separate repo, tracked independently
shopify/

# Temporary / scratch — never commit
.cursor/tmp/
.agents/skills.bak.*/
**/*.bak
**/*.tmp

# Generated ops sync output
ops-hub/brands/*/shopify-snapshot.json

# Personal IDE workspace files (machine-specific folder paths)
*.code-workspace
